const featureDb=supabase.createClient(window.CMJ_SUPABASE_URL,window.CMJ_SUPABASE_KEY);const fEsc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));async function initIdeaBox(){const form=document.querySelector('#idea-form');if(!form)return;const msg=document.querySelector('#idea-message');form.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(form);const {error}=await featureDb.from('ideas').insert({first_name:fd.get('first_name')||null,age:fd.get('age')?Number(fd.get('age')):null,email:fd.get('email')||null,category:fd.get('category')||'Autre',title:fd.get('title'),description:fd.get('description'),consent:true,status:'new'});msg.textContent=error?"Impossible d'envoyer l'idée pour le moment.":'Merci ! Ton idée a bien été envoyée au CMJ.';if(!error)form.reset();});}async function initCalendar(){const root=document.querySelector('#calendar-list');if(!root)return;const {data,error}=await featureDb.rpc('list_public_events');if(error){root.innerHTML='<div class="notice">Impossible de charger le calendrier.</div>';return;}const future=(data||[]).filter(e=>new Date(e.event_date)>=new Date()).slice(0,30);if(!future.length){root.innerHTML='<div class="notice">Aucune date programmée pour le moment.</div>';return;}root.innerHTML=future.map(e=>{const d=new Date(e.event_date);return `<article class="calendar-item"><div class="calendar-date"><strong>${String(d.getDate()).padStart(2,'0')}</strong><span>${fEsc(new Intl.DateTimeFormat('fr-FR',{month:'short'}).format(d))}</span></div><div><h3>${fEsc(e.title)}</h3><p>${fEsc(e.description)}</p><div>📍 ${fEsc(e.location||'Lieu à préciser')}</div></div></article>`}).join('');}document.addEventListener('DOMContentLoaded',()=>{initIdeaBox();initCalendar();});